@model IEnumerable<CoverType>

@{
    Layout = "_Layout";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover Type List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>&nbsp; Create New Cover Type</a>
        </div>
    </div>


<table class="table table-striped table-border">
    <tr>
        <th>Name</th>
        <th>Actions</th>
    </tr>
    
        @foreach(var obj in Model)
        {
            <tr width="50%">
            <td>@obj.Name</td>
            <td>
                <div class="btn-group" role="group">
                    <a style="width:90px" class="btn btn-secondary" asp-action="Edit" asp-controller="CoverType" asp-route-id=@obj.Id>
                        <i class="bi bi-pencil-square"></i>Edit</a>
                        &nbsp;
                    <a style="width:90px" class="btn btn-danger" asp-action="Delete" asp-controller="CoverType" asp-route-id=@obj.Id>
                        <i class="bi bi-trash3-fill"></i>Delete</a>
                </div>
            </td>
            </tr>
        }
    
</table>
</div>

